Implementation
==============

Built using Flutter (frontend) and Firebase (backend). The UI is structured using `recipe.dart`, `recipeTesting.dart`, and pages for search, bookmarks, meal plans, and more.
